version: '3.8'
services:
  sana:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./notes:/app/notes
      - ./sana.db:/app/sana.db
    environment:
      - JWT_SECRET=${JWT_SECRET:-change-me-in-prod}
      - PORT=8080
